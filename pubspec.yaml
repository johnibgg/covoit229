name: covoit229
description: "Covoit229 — covoiturage solidaire au Bénin. Conducteurs et passagers s'entraident : gratuit, partage carburant ou montant libre."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  supabase_flutter: ^2.8.0
  url_launcher: ^6.3.0
  http: ^1.2.0
  image_picker: ^1.1.2
  intl: any
  google_fonts: ^6.2.1
  geolocator: ^12.0.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
